services:
  ticketa:
    container_name: ticketa
    build: .
    ports:
      - "8080:8080"
    environment:
      PG_USER: ${PG_USER}
      PG_PASSWORD: ${PG_PASSWORD}
      PG_DATABASE: ${PG_DATABASE}
      PG_PORT: ${PG_PORT}
      PG_HOST: database
      SERVER_PORT: 8080
    depends_on:
      database:
        condition: service_healthy
    networks:
      - ticketa

  database:
    container_name: ticketa-database
    image: "postgres:18.4"
    environment:
      POSTGRES_USER: ${PG_USER}
      POSTGRES_PASSWORD: ${PG_PASSWORD}
      POSTGRES_DB: ${PG_DATABASE}
    volumes:
      - ticketa_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PG_USER} -d ${PG_DATABASE}"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - ticketa

networks:
  ticketa:
    internal: true

volumes:
  ticketa_postgres_data:
